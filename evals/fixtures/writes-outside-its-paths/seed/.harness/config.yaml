version: 1
defaults:
  adapter: mock
  profile: worktree
  max_parallel: 1
adapters:
  mock:
    type: mock
    tasks:
      T-001:
        files:
          src/ok.py: |
            1
          elsewhere.py: |
            2
command_policy:
  default: deny
  rules:
  - match: '^git '
    verdict: allow
