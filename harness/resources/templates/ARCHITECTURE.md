# Architecture — (project name)

> **UNFILLED TEMPLATE.**
>
> Fill this in and delete this block. It tells a coding agent where your code
> lives and why it is shaped the way it is.

Write in whatever language your team works in.

## Directory layout

```
(your project's own source tree)
```

If this repository also carries the Harness Framework itself, its directories
(`harness/`, `docs/`, `tests/`, `evals/`) are infrastructure rather than your
project. Keep your source in its own top-level directory so a reader can tell the
two apart at a glance.

## Patterns

(The handful of shapes the code repeats. Where logic is allowed to live, where it
is not, what talks to what.)

## Data flow

```
(input → ... → output, for the path that matters most)
```

## Decisions

One row per decision worth defending later. The cost column is the one that saves
an argument.

| # | Decision | Why | What it costs |
|---|---|---|---|
| 1 | | | |
| 2 | | | |
| 3 | | | |
